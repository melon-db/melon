plugins {
    id 'java'
}

dependencies {
    compile rootProject
    compile 'com.h2database:h2:1.4.197'
}

jar {
    from {
        configurations.compile.collect { it.isDirectory() ? it : zipTree(it) }
    }
}
