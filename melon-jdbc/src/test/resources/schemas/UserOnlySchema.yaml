name: dummy
tables:
  - name: User
    columns:
      - name: id
      - name: firstName
      - name: lastName
      - name: additional
    storage:
      location: ../data/User.csv
      properties:
        ignoreHeader: true
