name: dummy
tables:
  - name: Address
    columns:
      - name: id
        primary: true
      - name: city
      - name: country
      - name: userId
    storage:
      path: data/AddressAttributes.xml
      properties:
        xml-record-path: /ADDRESS/DATA_RECORD
        xml-format: Attributes
