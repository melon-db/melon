name: dummy
tables:
  - name: User
    columns:
      - name: id
        primary: true
      - name: firstName
      - name: lastName
      - name: additional
    storage:
      location: ../data/User.csv
      properties:
        csv-header: true
  - name: Team
    columns:
      - name: id
        primary: true
      - name: name
    storage:
      location: ../data/Team.properties
  - name: TeamMembers
    columns:
      - name: teamId
        primary: true
      - name: userId
        primary: true
    storage:
      location: ../data/TeamMembers.csv
      properties:
        csv-header: false
