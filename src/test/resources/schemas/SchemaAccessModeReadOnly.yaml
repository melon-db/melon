name: dummy
properties:
  storage-mode: ReadOnly
tables:
  - name: User
    columns:
      - name: id
        primary: true
      - name: firstName
      - name: lastName
      - name: additional
    storage:
      uri: ../data/User.csv
      properties:
        csv-header: false
