name: dummy
properties:
  storage-accessMode: ReadOnly
tables:
  - name: User
    columns:
      - name: id
        primary: true
      - name: firstName
      - name: lastName
      - name: additional
    storage:
      path: ../data/User.csv
      properties:
        csv-header: false
